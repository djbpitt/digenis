start = prepositions
prepositions = element prepositions { preposition+ }
preposition = element preposition { lemma, gloss, empty }
lemma =
  attribute lemma {
    xsd:string { pattern = ".+" }
  }
gloss = attribute gloss { text }
